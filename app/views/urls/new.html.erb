<%= render "form" %>

<table class="table table-striped"> 
  <thead> 
    <tr> 
      <th></th> 
      <th>Long Url</th> 
      <th>Short Url</th> 
      <th>Created</th> 
    </tr> 
  </thead>

  <tbody>
    <% if @user_urls.any? %>
      <%= render "user_urls" %>
    <% else %>
      <tr>
        <td colspan="3">No Urls added yet!</td>
      </tr>
    <% end %>
  </tbody> 
</table>

<%= will_paginate @user_urls %>
